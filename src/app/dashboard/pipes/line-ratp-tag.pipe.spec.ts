import { LineRatpTagPipe } from './line-ratp-tag.pipe';

describe('LineRatpTagPipe', () => {
  it('create an instance', () => {
    const pipe = new LineRatpTagPipe();
    expect(pipe).toBeTruthy();
  });
});
