import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { WidgetMoviesComponent } from './widget-movies.component';

describe('WidgetMoviesComponent', () => {
  let component: WidgetMoviesComponent;
  let fixture: ComponentFixture<WidgetMoviesComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ WidgetMoviesComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(WidgetMoviesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
