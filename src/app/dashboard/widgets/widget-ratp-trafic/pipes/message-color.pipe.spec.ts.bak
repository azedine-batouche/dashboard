import { MessageColorPipe } from './message-color.pipe';

describe('MessageColorPipe', () => {
  it('create an instance', () => {
    const pipe = new MessageColorPipe();
    expect(pipe).toBeTruthy();
  });
});
