import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { WidgetBitbucketComponent } from './widget-bitbucket.component';

describe('WidgetBitbucketComponent', () => {
  let component: WidgetBitbucketComponent;
  let fixture: ComponentFixture<WidgetBitbucketComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ WidgetBitbucketComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(WidgetBitbucketComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
